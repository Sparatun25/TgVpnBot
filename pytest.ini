[pytest]
# Добавляем корень проекта в sys.path, чтобы тесты могли импортить
# core.*, api.*, services.*, database.* как из самого приложения.
pythonpath = .
testpaths = tests
# Подробный вывод при падении: locals, traceback, comparisons.
addopts = -ra --tb=short
